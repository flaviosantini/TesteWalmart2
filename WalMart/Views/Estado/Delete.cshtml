@model Walmart.Entidades.Estado

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Estado</legend>

    <div class="display-label">CodEstado</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CodEstado)
    </div>

    <div class="display-label">Pais</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Pais)
    </div>

    <div class="display-label">Nome</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Nome)
    </div>

    <div class="display-label">Sigla</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Sigla)
    </div>

    <div class="display-label">Regiao</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Regiao)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
