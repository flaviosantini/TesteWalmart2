@model Walmart.Entidades.Cidade

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Cidade</legend>

    <div class="display-label">CodCidade</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CodCidade)
    </div>

    <div class="display-label">CodEstado</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CodEstado)
    </div>

    <div class="display-label">Nome</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Nome)
    </div>

    <div class="display-label">Capital</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Capital)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
