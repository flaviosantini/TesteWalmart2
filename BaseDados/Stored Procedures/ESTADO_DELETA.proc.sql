USE WALMART
GO

IF EXISTS (SELECT * FROM sys.objects WHERE type = 'P' AND name = 'ESTADO_DELETA')
DROP PROCEDURE ESTADO_DELETA
GO

CREATE PROCEDURE [dbo].[ESTADO_DELETA]
	@COD_ESTADO	INT

AS
BEGIN

	DELETE FROM ESTADO
	WHERE
		COD_ESTADO = @COD_ESTADO
END
GO

