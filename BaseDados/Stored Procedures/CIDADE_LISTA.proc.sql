USE WALMART
GO

IF EXISTS (SELECT * FROM sys.objects WHERE type = 'P' AND name = 'CIDADE_LISTA')
DROP PROCEDURE CIDADE_LISTA
GO

CREATE PROCEDURE [dbo].[CIDADE_LISTA]

AS
BEGIN

	SELECT COD_CIDADE, COD_ESTADO, NOME, IC_CAPITAL
	FROM CIDADE

END
GO

